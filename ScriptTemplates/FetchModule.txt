using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Mirror;
using RequestForMirror;

public partial class $CLASSNAME$
{
    [Command]
    protected override void CmdComputeResponse(NetworkConnectionToClient sender = null)
    {
        HandleRequestOnServer(out var status, out $BASE_GENERIC_ARGUMENT$ response, sender);
        TargetReceivedResponse(connectionToClient, status, response);
    }
    
    [TargetRpc]
    public void TargetReceivedResponse(NetworkConnection target, RequestStatus status, $BASE_GENERIC_ARGUMENT$ response)
    {
        HandleResponseOnClient(status, response);
    }
}
