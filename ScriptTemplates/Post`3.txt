using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Mirror;
using RequestForMirror;

public partial class $CLASSNAME$
{
    [Command]
    protected override void CmdComputeResponse($BASE_GENERIC_ARGUMENT$ requestData, $BASE_GENERIC_ARGUMENT_2$ requestData2, NetworkConnectionToClient sender = null)
    {
        HandleRequestOnServer(requestData, requestData2, out var status, out $BASE_GENERIC_ARGUMENT_3$ response, sender);
        TargetReceivedResponse(connectionToClient, status, response);
    }
    
    [TargetRpc]
    public void TargetReceivedResponse(NetworkConnection target, RequestStatus status, $BASE_GENERIC_ARGUMENT_3$ response)
    {
        HandleResponseOnClient(status, response);
    }
}
